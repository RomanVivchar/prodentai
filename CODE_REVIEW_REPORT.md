# Отчет о Code Review проекта ProDentAI

## 🔴 Критические проблемы безопасности

### 1. Хардкод SECRET_KEY в auth.py
**Файл:** `backend/routers/auth.py:15`
```python
SECRET_KEY = "your-secret-key-change-in-production"
```
**Проблема:** Секретный ключ захардкожен в коде, что критично для безопасности.
**Решение:** Использовать переменную окружения `SECRET_KEY` из `.env`.

### 2. Отсутствие passlib в requirements.txt
**Файл:** `backend/requirements.txt`
**Проблема:** `passlib` используется в `auth.py`, но отсутствует в зависимостях.
**Решение:** Добавить `passlib[bcrypt]` в requirements.txt.

---

## 🟠 Критические ошибки кода

### 3. Отсутствующий файл scheduler.py
**Файл:** `telegram_bot/main.py:22`
```python
from scheduler import reminder_scheduler
```
**Проблема:** Файл `scheduler.py` не существует, что приведет к ошибке при запуске бота.
**Решение:** Создать файл `telegram_bot/scheduler.py` или удалить импорт.

### 4. Отсутствующий модуль auth_utils
**Файлы:** 
- `backend/routers/risk_assessment.py:41`
- `backend/routers/users.py:41`
```python
from auth_utils import get_user_by_id
```
**Проблема:** Модуль `auth_utils` не существует.
**Решение:** Создать файл `backend/routers/auth_utils.py` или использовать прямые запросы к БД.

### 5. Несоответствие моделей базы данных

#### 5.1. RiskAssessment модель
**Файл:** `backend/models.py`
**Проблема:** В модели определено только поле `risk_data` (Text), но в коде используются:
- `assessment_data` (в `risk_assessment.py:140`)
- `risk_scores` (в `risk_assessment.py:141, 115`)
- `recommendations` (в `risk_assessment.py:142`)

**Решение:** Обновить модель, добавив необходимые поля:
```python
assessment_data = Column(Text)  # JSON string
risk_scores = Column(Text)  # JSON string
recommendations = Column(Text)  # JSON string или JSON array
```

#### 5.2. Reminder модель
**Файл:** `backend/models.py`
**Проблема:** В модели определены только `time` и `enabled`, но в коде используются:
- `reminder_type` (в `reminders.py:66`)
- `date` (в `reminders.py:68`)
- `message` (в `reminders.py:69`)
- `is_active` (в `reminders.py:82`) - используется вместо `enabled`

**Решение:** Обновить модель:
```python
reminder_type = Column(String)
date = Column(String, nullable=True)
message = Column(Text, nullable=True)
is_active = Column(Boolean, default=True)  # или переименовать enabled в is_active
```

#### 5.3. PsychologySession модель
**Файл:** `backend/models.py`
**Проблема:** В модели только `messages`, но в `database_utils.py` используются:
- `session_type`
- `user_message`
- `ai_response`
- `sentiment_score`

**Решение:** Обновить модель или изменить запросы в `database_utils.py`.

#### 5.4. Отсутствующие модели
**Файлы:** 
- `backend/database.py:47-54` импортирует модели, которых нет в `models.py`:
  - `BracesFAQ`
  - `Fact`
  - `NutritionLog`

**Решение:** Добавить эти модели в `models.py` или удалить импорты.

### 6. Несоответствие типов сессий (Session vs AsyncSession)

**Файлы:**
- `backend/routers/auth.py` - использует `Session` вместо `AsyncSession`
- `backend/routers/nutrition.py` - использует `Session` вместо `AsyncSession`
- `backend/routers/psychology.py` - использует `Session` вместо `AsyncSession`

**Проблема:** Смешивание синхронных и асинхронных сессий может привести к ошибкам.
**Решение:** Заменить все `Session` на `AsyncSession` и использовать `await` для всех операций с БД.

### 7. Неправильное использование SQL в health check
**Файл:** `backend/main.py:226`
```python
await db.execute("SELECT 1")
```
**Проблема:** Используется строка вместо `text()`.
**Решение:** 
```python
from sqlalchemy import text
await db.execute(text("SELECT 1"))
```

### 8. Отсутствующие функции в ml_services
**Файлы:**
- `backend/routers/nutrition.py:27` - `analyze_nutrition_text`
- `backend/routers/nutrition.py:44` - `analyze_nutrition_image`
- `backend/routers/psychology.py:35` - `chat_with_psychologist`

**Проблема:** Эти функции не определены в `shared/ml_services.py`.
**Решение:** Добавить функции или использовать `MLServiceManager` напрямую.

---

## 🟡 Проблемы конфигурации

### 9. Дублирующийся ключ в config.py
**Файл:** `telegram_bot/config.py:48-49`
```python
"facts": f"{backend_url}/api/facts",
"facts": f"{backend_url}/api/facts",  # Дубликат
```
**Решение:** Удалить дубликат.

### 10. Несоответствие полей User модели
**Файл:** `backend/models.py`
**Проблема:** В `users.py:73-74` используются поля `first_name` и `last_name`, которых нет в модели.
**Решение:** Добавить эти поля в модель `User` или удалить их использование.

---

## 🟢 Предупреждения и улучшения

### 11. CSS предупреждения
**Файл:** `web/src/pages/Nutrition.tsx:309, 334`
**Проблема:** Конфликтующие CSS классы `block` и `flex`.
**Решение:** Удалить один из классов.

### 12. Неиспользуемые импорты
**Файл:** `backend/main.py:11, 49, 54`
```python
import types  # Дважды импортируется
import importlib.util  # Дважды импортируется
```
**Решение:** Удалить дублирующиеся импорты.

### 13. Отсутствие обработки ошибок
**Файл:** `backend/main.py:226`
**Проблема:** Нет обработки ошибок при проверке подключения к БД.
**Решение:** Улучшить обработку ошибок.

### 14. Неправильный тип в toggle_reminder
**Файл:** `backend/routers/reminders.py:112`
**Проблема:** Функция принимает только `reminder_id`, но в телеге передается JSON с `is_active`.
**Решение:** Обновить сигнатуру функции или изменить вызов в телеграм-боте.

---

## 📋 Резюме

**Критические проблемы:** 8
**Важные проблемы:** 6
**Предупреждения:** 4

**Приоритет исправления:**
1. 🔴 Критические проблемы безопасности (1-2)
2. 🔴 Критические ошибки кода (3-8)
3. 🟡 Проблемы конфигурации (9-10)
4. 🟢 Предупреждения (11-14)

